<% if @user_array.present? %>
    <%= select_tag 'name', options_for_select(@user_array.collect) %>
    <%= button_to "フォローする", relationships_path, params: {user_id: @user_array.first[1]}, method: :post %>
<% else %>
    <p>フォローしていないユーザーは存在しません。</p>
<% end %>